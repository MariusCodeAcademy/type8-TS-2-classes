import Employee from './Employee';
import Partner from './Partner';

export default class Ui {
  private static _workers: (Employee | Partner)[] = [];

  // static addWorker(worker: Employee | Partner) {}

  // static generateRow() {}
}
// const ui = new Ui()
// Ui.addWorker();
